"""Modern multi-agent ticket triage sample using Azure AI Foundry prompt agents.

This module mirrors the old "connected agents" exercise concept, but uses the
current AIProjectClient + prompt agent + responses flow instead of classic
ConnectedAgentTool orchestration.
"""
